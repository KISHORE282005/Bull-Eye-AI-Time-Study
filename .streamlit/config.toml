[server]
# Megabytes. Streamlit buffers the whole upload in RAM, so the host
# needs headroom of roughly this size on top of normal usage.
maxUploadSize = 10240
port= 4002
headless = true

[theme]
base="light"
backgroundColor="#f7f4f4"
secondaryBackgroundColor="#f7f4f4"
textColor="#c53333fb"

